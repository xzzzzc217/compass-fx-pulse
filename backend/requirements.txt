flask>=3.0.0
flask-cors>=4.0.0
mysql-connector-python>=9.0.0
python-dotenv>=1.0.0
openai>=1.50.0
requests>=2.32.0

# Phase 3 RAG (heavy: ~3GB extra weights from llm/models/, only needed if you use search_forex_knowledge)
chromadb>=0.5
FlagEmbedding>=1.3
pypdf>=5.0

# Phase 3.3 MCP server
mcp>=1.2
httpx>=0.27

# Phase 4.1 Caching layer (default backend is stdlib-only; Redis optional)
redis>=5.0  # optional; only used if REDIS_URL env var is set
